
from overview import Overview
from newCLS import ChooseInMenu
from time import perf_counter, sleep

from typing import Callable

class MenuOne(object):

       
    def __init__(self,select:int|str,textInput:str='0',menu_list:list[str]=['1. overviewing tab', '2. something','3. something']):
    
        
        self._select = select
        self._textInput = textInput
        self._menu_list = menu_list
        

@property 
def menu(self):
    print(f'{self._select} being accessed')
    

    @menu.setter
    def menu (self,value:str):
        print('f{self._select} being accessed')
        






if (__name__ == '__main__'):
    inMenu : S1electInMenu = S1electInMenu()
    inMenu.print_menu(['1. overviewing tab', '2. something','3. something'])
  
   
    def jump_to_script():
        while overviewtap_input := input('type to start script: '):
                 if overviewtap_input == 'start':
                                print("script_started")
                                start:   Overview =  Overview.turn_on('start')
                 else:
                        print('exit')    
        if overviewtap_input == "start":
                start : 
  
    def printMenu(self):
        text = '0'
        while textInput := input('type the number of the tab you want to go to: '):
                text = textInput
        
         
                if text == '1':
                       
                                jump_to_script()
                       
                            
                                print('do nothing')
                elif text == '2'
                        print('your in overview tab2')
                elif text == '3':
                        print('your in overview tab3')
                
       
                else:
                        print('exit')
  
def repeat():
        def wrapper():
            return vars
        return 
        return times
        
   # overview: Overview = Overview(0, 'overviewing tab')

    
def do_something(param):
        
        sleep(1)
        print(param)  

        
printMenu(['1. overviewing tab', '2. something','3. something'])


print(selected._menu_list)


def go_to(_select:int):
        print('go to {self._select}')
        
class S1electInMenu(MenuOne):
   def __init__(self, select:int):
    
    super().__init__(select)
   
    
    printMenu(['1. overviewing tab', '2. something','3. something'])    
    
    
    
