import matplotlib.pyplot as plt
import numpy as np
import psutil
from time import sleep
def mainGrapg():
    
        def init(self, freq:list,lines:list):
            self.freq = freq
            self.lines = [*self._get_lines(self, *args, data=data, **kwargs)]
            
            
        if __name__ == '__main__':
            
            while True:
                i=+1
                freqency = str(psutil.cpu_freq().current)
           
                xpoints = np.array([...,i])    

                ypoints = np.array([freqency,...])
                sleep(10)
                plt.plot(xpoints, ypoints)
                plt.show()